name: smartmeter_app
description: Application de surveillance de compteur intelligent
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.63.0
  shared_preferences: ^2.2.0

  langchain: ^0.7.0
  langchain_openai: ^0.7.3
  dash_chat_2: ^0.0.21
  http: ^1.2.2
  flutter_dotenv: ^5.1.0  # Pour gérer la clé API

  #flutter_local_notifications: ^14.0.0+1
  #speech_to_text: ^6.3.0
  flutter_tts: ^3.8.3

  firebase_core: ^3.13.1
  firebase_auth: ^5.5.4
  google_sign_in: ^6.1.6
  provider: ^6.0.5

  cloud_firestore: ^5.6.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/config/.env